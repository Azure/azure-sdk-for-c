{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cd Debug && rm -rf * && cmake -DUNIT_TESTING=ON -DCMAKE_BUILD_TYPE=Debug  .. && make"
        }
    ]
}
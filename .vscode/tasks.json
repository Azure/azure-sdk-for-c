{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cd Debug && rm -rf * && cmake -Duse_default_uuid=ON -DCMAKE_BUILD_TYPE=Debug  .. && make"
        }
    ]
}
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cd Debug && rm -rf * && cmake -DBUILD_AZ_KEY_VAULT_CLIENT=ON -DCMAKE_BUILD_TYPE=Debug  .. && make"
        }
    ]
}